@using anonymous_forum_csharp.Models.ViewModels
@using anonymous_forum_csharp.Models
@model ThreadIndexViewModel

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="text-center">
        @foreach (var item in Model.PostList)
        {
        <a href="@Url.Action("Index", "Comment", new { Id = item.Id})">
            <div class="post-box">
            <h4>@item.Title</h4>
            <p>@item.Text</p>
            </div>
        </a>
        }
</div>
@* @using (Html.BeginForm("Index", "Thread"))
{
    @Html.LabelFor(m => m.Post.Title)
    @Html.EditorFor(m => m.Post.Text)
    <input type="submit" value="Create" />
} *@

@await Html.PartialAsync("ThreadPostBox", Model.ThreadCreate)


